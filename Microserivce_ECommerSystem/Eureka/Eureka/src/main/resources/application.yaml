server:
  port: 8761

spring:
  application:
    name: EUREKA-SERVER

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false # tắt chế độ bảo vệ
    # eviction-interval-timer-in-ms: 3000 # kiểm tra instance mỗi 3 giây
